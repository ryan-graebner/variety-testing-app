class DatabaseService {
  /*
  - Initializes the database
  - Creates schema if empty
  - Erase all data
  - Read data from .csv file(s) and convert it into SQL Insert queries
  - Fetches data
  - Converts all data into objects for the DataRepository
   */

  final String testProvider = "String from the DatabaseService";
}